---
description: Show how the auto-DMs are performing and what to change
---

Run `node bin/autodm.mjs stats --json` and `node bin/autodm.mjs list --json`, then give the
user a short read on what's working.

Say which keyword is pulling, which automation has failed sends worth investigating, and
what the click-through rate actually means (unique clicks ÷ trackable sends — Instagram
sends no delivery receipt, so `read` is the closest thing to delivery confirmation).

End with one concrete suggestion, not a list of five.

$ARGUMENTS
