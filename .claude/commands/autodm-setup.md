---
description: Set up Instagram auto-DM from scratch — Zernio account, API key, Instagram connection, first automation
---

Walk the user through setting up this repo end to end. Use the `autodm` skill.

Start by running `node bin/autodm.mjs doctor --json` and pick up from whatever is missing.
Do not assume they have done any of the earlier steps.

Run every command yourself and report results in plain language. They should never need to
type a command unless they want to.

$ARGUMENTS
